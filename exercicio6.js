let nascimento = parseInt(prompt("Digite o seu ano de nascimento:"))

let idade = 2024-nascimento;
    
    if( idade >=18 ) {
        alert("Sua idade foi aprovada!!!");
    } else {
        alert("Voce não tem permissão de acesso, devido a restrições de idade!")
    }